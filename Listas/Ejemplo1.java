package Listas;

public class Ejemplo1 {
    public static void main(String[] args) {
        /*Listas en java
        * ArrayList()
        * Es una estructura de datos en forma de lista que
        permite alamcenar elementos de un mismo tipo de dato incluso
        la lista puede almacenar Objetos.
        Metodos pre establecidos:
        - add(elemento)
        - add(indice, elemento)
        - clear()
        - contains(elemento)
        - get(indice)
        - indexOf(elemento)
        - isEmpty()
        - remove(indice)
        - remove(elemento)
        - removeIf(filtro)
        - set(indice, elemento)
        - size()
        - toArray()
        */
    }
}
