package Diccionarios;

public class Ejemplo1 {
    public static void main(String[] args) {
     // Un diccionario en java contiene una serie de elementos que trabajan como entrada
     // y estas estan formadas por un par (clave, valor), la clave como key permite 
     // acceder al valor, las claves son unicas.
     // En java existen varios tipos de diccionario HashMap, EnumMap, HashTable, entre otros
     // HashMap
     // metodos:
     // - get(clave)
     // - put(clave, valor)
     // - keySet()
     // - values()
     // - entrySet()
     // - containsKey(clave)
     // - getkey()
     // - getValue()

    }
}
